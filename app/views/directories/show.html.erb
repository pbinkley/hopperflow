<h1>HopperFlow</h1>

<p id="notice"><%= notice %></p>

<p>
  <strong>Dirpath:</strong>
  <%= @directory.dirpath %>
</p>

<h2>Images</h2>
<ul>
	<% @images.each do |image| %>
		<li><%= image.basename %></li>
	<% end %>
</ul>
<%= link_to 'Edit', edit_directory_path(@directory) %> |
<%= link_to 'Back', directories_path %>
